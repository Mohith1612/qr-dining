# Binary output
bin/
*.exe
*.out

# Environment files — never commit secrets
.env
.env.local

# Go tooling
vendor/

# IDE and editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS artifacts
.DS_Store
Thumbs.db

# Coverage and test output
cover.out
cover.html
*.test

# Docker
docker-compose.override.yml

# Backend binary
backend/server
# Compiled server binary at repo root (build artifact — never commit)
/server

# Frontend
frontend/.next/
frontend/.open-next/
frontend/.wrangler/
.wrangler/
frontend/out/
frontend/node_modules/
frontend/.env.local

# Session handouts (internal use)
handout.md

# E2E tooling output (regenerable)
e2e/node_modules/
e2e/artifacts/
e2e/screenshots/*/
.playwright-mcp/

# Alerting secrets — Telegram bot token and the dead man's switch ping URL.
# Both are capabilities; this repo is public with push protection on.
deploy/observability/secrets/

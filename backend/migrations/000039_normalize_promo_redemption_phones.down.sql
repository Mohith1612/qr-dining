-- Irreversible by design: the original non-canonical strings are not recoverable
-- from the normalized values, and restoring them would re-open the per-phone
-- promo cap bypass. Down is a no-op.
SELECT 1;
